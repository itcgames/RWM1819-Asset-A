/* global Component1, describe, it, expect, should */

describe('Component1()', function () {
  'use strict';

  it('MyImage Class', function () {
    expect(MyImage).to.be.a('function');
  });
  
});
